services:
  mongodb:
    image: mongo:latest
    
    container_name: mongodb
    
    ports:
      - "27007:27017" 
    
    volumes:
      - ./mongodata:/data/db

    networks: 
      backend:
    
    restart: always

networks: 
      backend: